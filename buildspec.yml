version: 0.2

phases:
  build:
    commands:
      - chmod +x ./gradlew
      - ./gradlew wrapper --gradle-version 6.0.1
      - ./gradlew assembleDebug
artifacts:
  files:
     - './android/app/build/outputs/**/*.apk'
  discard-paths: yes
